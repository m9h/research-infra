# AGENTS.md — {{ project_name }}

> Machine-readable project specification for AI coding assistants.
> Generated by research-infra. Last updated: {{ date }}.

## Project Identity

- **Name**: {{ project_name }}
- **Description**: {{ description }}
- **Language**: {{ language }}
- **Build system**: {{ build_system }}
- **Package manager**: uv
- **License**: {{ license }}
{% if repo_url %}- **Repository**: {{ repo_url }}{% endif %}

## Quick Start

```bash
cd {{ project_path }}
uv sync
uv run pytest
```

## Module Map

| Module | Purpose | Public API |
|--------|---------|------------|
{% for mod in modules -%}
| `{{ mod.path }}` | {{ mod.purpose }} | {{ mod.api }} |
{% endfor %}

## Testing

- **Framework**: pytest
- **Test location**: {{ test_path }}/
- **Run**: `uv run pytest {{ test_path }}/ -v`
- **No mocks**: Tests use real implementations, not unittest.mock.

## Dependencies

{% if internal_deps %}### Internal (within ~/dev/)
{% for dep in internal_deps -%}
- {{ dep.name }} ({{ dep.path }})
{% endfor %}{% endif %}
### External
{% for dep in external_deps -%}
- {{ dep.name }} {{ dep.version }}
{% endfor %}

## Architecture Invariants

{% for inv in invariants -%}
- {{ inv }}
{% endfor %}

## Manuscript Integration

This project uses research-infra for manuscript assembly. Markdown files with
YAML frontmatter (`category: research`, `section: <section>`) are automatically
discovered and assembled into a manuscript via `rinf build manuscript`.

See `manuscript/config.yaml` for project metadata and `manuscript/references.bib`
for bibliography.
